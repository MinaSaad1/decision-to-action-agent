import {ClipboardCheck} from 'lucide-react';import type {AuditEvent} from '../engine/reducer';import type {PreparedAction} from '../engine/types';
export function ActionRecord({action,audit}:{action:PreparedAction;audit:AuditEvent[]}){const heading=action.status==='escalated'?'Escalation prepared':action.status==='returned'?'Clarification prepared':'Action prepared and approved';return <section className="action-record"><div className="action-main"><ClipboardCheck size={24}/><div><p className="kicker">Prepared action</p><h3>{heading}</h3><b>{action.title}</b><p>{action.body}</p><small>Destination: {action.destination}</small><div className="no-write">Prepared only. No external system was changed.</div></div></div><div className="audit"><h4>Audit trail</h4>{audit.map((a,i)=><div key={a.id}><span>{String(i+1).padStart(2,'0')}</span><p><b>{a.label}</b><small>{a.detail}</small></p></div>)}</div></section>}
