import {render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {beforeEach,describe,expect,it} from 'vitest';import {App} from '../app/App';
beforeEach(()=>Object.defineProperty(window,'matchMedia',{writable:true,value:()=>({matches:true,addListener(){},removeListener(){}})}));
describe('proof interface',()=>{it('features procurement and switches packs',async()=>{const u=userEvent.setup();render(<App/>);expect(screen.getAllByText('Featured walkthrough')).toHaveLength(2);await u.click(screen.getByRole('tab',{name:/recruitment/i}));expect(screen.getByRole('heading',{name:'Recruitment'})).toBeInTheDocument()});it('runs to human review and approves',async()=>{const u=userEvent.setup();render(<App/>);await u.click(screen.getByRole('button',{name:/run procurement agent/i}));expect(await screen.findByText(/ready for human review/i)).toBeInTheDocument();await u.click(screen.getByRole('button',{name:/approve recommendation/i}));expect(screen.getByText(/action prepared and approved/i)).toBeInTheDocument();expect(screen.getByText(/audit trail/i)).toBeInTheDocument()})});
