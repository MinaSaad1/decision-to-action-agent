import {ArrowUpRight,CornerUpLeft,ShieldAlert} from 'lucide-react';import type {ReviewDecision} from '../engine/types';
export function HumanGate({enabled,onReview}:{enabled:boolean;onReview:(d:ReviewDecision)=>void}){return <section className="human-gate"><div><p className="kicker">Human gate</p><h3>Judgment stays accountable.</h3><p>The agent recommends. A person owns the consequential action.</p></div><div className="gate-actions"><button disabled={!enabled} className="approve" onClick={()=>onReview('approved')}>Approve recommendation <ArrowUpRight size={16}/></button><button disabled={!enabled} onClick={()=>onReview('returned')}><CornerUpLeft size={16}/> Return for clarification</button><button disabled={!enabled} onClick={()=>onReview('escalated')}><ShieldAlert size={16}/> Escalate exception</button></div></section>}
