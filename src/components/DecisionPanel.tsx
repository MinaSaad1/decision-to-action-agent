import {AlertTriangle,CheckCircle2,ExternalLink} from 'lucide-react';import type {DecisionPacket} from '../engine/types';
export function DecisionPanel({decision}:{decision:DecisionPacket}){const focus=(id:string)=>document.getElementById(`source-${id}`)?.focus();return <section className="decision"><div className="section-heading"><span>Decision packet</span><small className={`confidence ${decision.confidence}`}>{decision.confidence} confidence</small></div><h3>{decision.recommendation}</h3><ul className="rationale">{decision.rationale.map(r=><li key={r}>{r}</li>)}</ul><div className="decision-grid"><div><h4>Policy checks</h4>{decision.policyChecks.map(c=><div className={`check ${c.result}`} key={c.id}>{c.result==='pass'?<CheckCircle2 size={16}/>:<AlertTriangle size={16}/>}<span><b>{c.label}</b><small>{c.detail}</small></span></div>)}</div><div><h4>Evidence</h4>{decision.evidence.map(e=><button className="evidence" key={e.sourceId} onClick={()=>focus(e.sourceId)}><span><b>{e.label}</b><small>“{e.excerpt}”</small></span><ExternalLink size={14}/></button>)}</div></div>{decision.missingInformation.length>0&&<div className="missing"><b>Missing information</b>{decision.missingInformation.join(' · ')}</div>}</section>}
