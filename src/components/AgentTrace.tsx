import {Check,LoaderCircle,LockKeyhole} from 'lucide-react';import {stageIds,type StageId} from '../engine/types';
const labels:Record<StageId,string>={intake:'Intake',understand:'Understand',evaluate:'Evaluate',explain:'Explain','human-gate':'Human gate','act-and-log':'Act + log'};
export function AgentTrace({active,completed,status}:{active:StageId|null;completed:StageId[];status:string}){return <section className="trace" aria-live="polite"><div className="section-heading"><span>Agent run</span><small>{status==='idle'?'Waiting':status==='review'?'Ready for human review':status}</small></div><ol>{stageIds.map((id,i)=>{const done=completed.includes(id),isActive=active===id;return <li key={id} className={done?'done':isActive?'active':'waiting'}><span className="trace-num">{done?<Check size={14}/>:isActive?<LoaderCircle size={14}/>:id==='human-gate'?<LockKeyhole size={14}/>:String(i+1).padStart(2,'0')}</span><b>{labels[id]}</b><small>{done?'Complete':isActive?'In progress':'Waiting'}</small></li>})}</ol></section>}
